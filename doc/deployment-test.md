# Deployment test marker

This harmless marker was added on 2026-08-22 to verify that the WordPress repository update screen can install a newer `soinproduction/php-kit` commit.

After a successful admin update, this file should exist in `vendor/soinproduction/php-kit/doc/deployment-test.md` on the target site.
