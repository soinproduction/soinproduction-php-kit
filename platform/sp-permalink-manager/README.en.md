# SP Permalink Manager

Adds Settings → Reading controls for removing selected custom post-type and taxonomy bases from public URLs. Enable it with `sp-permalink-manager`.

The module filters generated links, repairs main-query/request parsing, and flushes rewrite rules when options change. Root-level slugs can collide with Pages, CPT items and terms; verify existing content and rollback routes before enabling it on a populated site.
