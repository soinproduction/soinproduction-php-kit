# Branding

Заменяет логотип экрана входа WordPress на `BUILD . 'img/logo.svg'`. Подключается platform-модулем `branding`.

Тема должна определить константу `BUILD` до загрузки platform-модулей PHP Kit. Стили добавляются только на экран входа через `login_head`.
