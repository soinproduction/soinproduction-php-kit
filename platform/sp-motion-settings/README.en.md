# SP Motion Settings

Adds a Settings → Theme Behavior switch for theme animations. Enable it with `sp-motion-settings`.

Public helpers:

- `sp_theme_animations_enabled(): bool`

The value is stored in `sp_enable_theme_animations` and defaults to enabled.
