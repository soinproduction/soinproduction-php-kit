# SP Motion Settings

Adds Settings → Theme Behavior switches for the page loader and theme animations. Enable it with `sp-motion-settings`.

Public helpers:

- `sp_page_loader_enabled(): bool`
- `sp_theme_animations_enabled(): bool`

Values are stored in `sp_enable_page_loader` and `sp_enable_theme_animations`; both default to enabled.
