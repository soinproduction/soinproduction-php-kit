# SP Duplicator License

Project-specific Duplicator Pro compatibility override used on Duplicator administration screens. Enable it only in environments that intentionally rely on this behavior.

The module reaches into Duplicator internals and can break after vendor updates. Treat it as a private compatibility layer, review it during every Duplicator upgrade, and never expose its implementation in public output or logs.
