# SP CF7 Redirects

Adds Default, Redirect and Modal metadata to the Contact Form 7 editor and emits matching `data-*` attributes on rendered form wrappers.

This module provides metadata only. It does not include a browser listener that redirects visitors or opens modals after submission; the consuming theme must implement and test that behavior.
