# SP CF7 UI Select

Provides `custom_select` as both a WordPress shortcode and a Contact Form 7 form tag, including tag-generator and mail-tag integration.

The rendered control requires the project's frontend Custom Select JavaScript to create its hidden submitted input. Required validation checks only for a nonempty value; add a server-side allow-list when the value affects pricing, permissions or other trusted logic.
