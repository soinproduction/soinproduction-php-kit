# SP CF7 Icon Generator

Adds a UI Icon tag-generator dialog to the Contact Form 7 editor.

The dialog reads `uploads/ui-icons/manifest.json` and inserts an `sp_icon` WordPress shortcode with the selected icon, dimensions and CSS class. It does not register a submitted CF7 form field; the shortcode is presentation-only and requires the project's UI Assets subsystem.
